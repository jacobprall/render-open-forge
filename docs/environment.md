# Environment Variables

## Local development

There is a single `.env` at the **repo root** that holds every variable for every
process. The per-package files are symlinks back to it:

```
.env                          # canonical
apps/web/.env          → ../../.env
apps/web/.env.local    → ../../.env
apps/agent/.env        → ../../.env
```

So `bun run web`, `bun run worker`, `next dev`, drizzle-kit, and the setup
scripts all read the same file. Edit the root `.env`, no per-package
duplication. The symlinks are checked into the repo (the targets — the actual
secret-bearing files — remain `.gitignore`d). On a fresh checkout, run
`cp .env.example .env` and fill values in.

The tables below describe what each **service** needs at deploy time on Render
(via `render.yaml`). In dev, the union of all of them lives in the root `.env`.

## Web service (`apps/web`)

| Variable | Required | Notes |
|---|---|---|
| `DATABASE_URL` | Yes | Auto-wired by `render.yaml` from `openforge-db` |
| `REDIS_URL` | Yes | Auto-wired by `render.yaml` from `openforge-redis` |
| `AUTH_SECRET` | Yes | NextAuth encryption key, auto-generated by `render.yaml` |
| `CSRF_SECRET` | **Yes (prod)** | CSRF token signing key. Auto-generated by `render.yaml`. Throws at startup if missing in production |
| `AUTH_URL` | **Yes (prod)** | Public URL of the web app — NextAuth uses this for OAuth callbacks. Set to your Render web URL (e.g. `https://openforge-web-xxxx.onrender.com`) |
| `NEXT_PUBLIC_APP_URL` | **Yes (prod)** | Public URL of the web app — used for OAuth redirect URIs, invite links, and CI callback URLs. Set to the same URL as `AUTH_URL` |
| `ADMIN_EMAIL` | Yes | Email for the auto-bootstrapped admin account |
| `ADMIN_PASSWORD` | Yes | Password for the auto-bootstrapped admin account |
| `GITHUB_OAUTH_CLIENT_ID` | Yes | GitHub OAuth App client ID for user authentication |
| `GITHUB_OAUTH_CLIENT_SECRET` | Yes | GitHub OAuth App client secret |
| `GITHUB_TOKEN` | No | GitHub PAT for server-side operations (webhook registration, etc.) |
| `GITHUB_WEBHOOK_SECRET` | No | Shared secret for verifying incoming GitHub webhooks |
| `ANTHROPIC_API_KEY` | Yes* | *Provider key required for LLM access unless keys are stored in Postgres (see `ENCRYPTION_KEY`) |
| `OPENAI_API_KEY` | No | Enables OpenAI models when set as env fallback |
| `ENCRYPTION_KEY` | Yes | 64 hex chars (`openssl rand -hex 32`), **same on `openforge-web` and `openforge-agent`** — encrypts UI-managed API keys at rest. Auto-generated by `render.yaml` and linked into agent/gateway via `fromService` |
| `GATEWAY_INTERNAL_URL` | Yes | Auto-wired (`http://openforge-gateway:4100`) — internal URL for the gateway service |
| `GATEWAY_API_SECRET` | Yes | Auto-wired from `openforge-gateway` via `fromService` |
| `RENDER_API_KEY` | Yes | Render API key for deploy tools and CI |
| `RENDER_WEBHOOK_SECRET` | No | Shared secret for verifying incoming Render deploy webhooks (enables reactive sessions) |
| `RENDER_CI_WORKFLOW_SLUG` | No | Default `openforge-ci`, must match your Workflow deployment slug |
| `CI_RUNNER_SECRET` | Yes | Auto-generated on web and synced to `openforge-ci` via Blueprint |
| `CI_CALLBACK_URL` | No | Override callback base URL if tasks cannot POST to the default app URL |
| `CI_RUNNER_MODE` | No | Omit or `render` on Render. Use `local` only for laptop dev |
| `FORGEJO_INTERNAL_URL` | No | Only needed if using self-hosted Forgejo git |
| `FORGEJO_EXTERNAL_URL` | No | Public URL of your Forgejo instance (self-hosted only) |
| `FORGEJO_OAUTH_CLIENT_ID` | No | Forgejo OAuth App client ID (self-hosted only) |
| `FORGEJO_OAUTH_CLIENT_SECRET` | No | Forgejo OAuth App client secret (self-hosted only) |
| `FORGEJO_AGENT_TOKEN` | No | Agent service account token (self-hosted only) |

## Agent worker (`apps/agent`)

| Variable | Required | Notes |
|---|---|---|
| `DATABASE_URL` | Yes | Auto-wired by `render.yaml` from `openforge-db` |
| `REDIS_URL` | Yes | Auto-wired by `render.yaml` from `openforge-redis` |
| `SANDBOX_SERVICE_HOST` | Yes | Auto-wired (`openforge-sandbox:3001`) |
| `SANDBOX_SHARED_SECRET` | Yes | Linked from `openforge-sandbox` via `fromService` in `render.yaml` |
| `SANDBOX_SESSION_SECRET` | Yes | Linked from `openforge-sandbox` via `fromService` in `render.yaml` |
| `GITHUB_TOKEN` | No | GitHub PAT for authenticated git operations |
| `ANTHROPIC_API_KEY` | Yes* | *Provider key required for LLM access unless keys are stored in Postgres (see `ENCRYPTION_KEY`) |
| `OPENAI_API_KEY` | No | Enables OpenAI models |
| `ENCRYPTION_KEY` | Yes | Linked from `openforge-web` via `fromService` — must match web |
| `RENDER_API_KEY` | No | Enables Render deploy/infrastructure tools |
| `RENDER_OWNER_ID` | No | Render owner ID for scoping API calls |
| `MAX_CONCURRENT_RUNS` | No | Default `5`; max parallel agent jobs per worker |
| `ASK_USER_TIMEOUT_SEC` | No | Default `900`; max time the worker blocks on `ask_user_question` |
| `FORGEJO_INTERNAL_URL` | No | Only needed if using self-hosted Forgejo git |
| `FORGEJO_SANDBOX_URL` | No | URL Forgejo uses inside the sandbox for git clones (self-hosted only) |
| `FORGEJO_AGENT_TOKEN` | No | Agent service account token (self-hosted only) |

## Gateway (`apps/gateway`)

| Variable | Required | Notes |
|---|---|---|
| `DATABASE_URL` | Yes | Auto-wired by `render.yaml` from `openforge-db` |
| `REDIS_URL` | Yes | Auto-wired by `render.yaml` from `openforge-redis` |
| `GATEWAY_API_SECRET` | Yes | Auto-generated — bearer token for authenticating all gateway requests |
| `GATEWAY_PORT` | No | Default `4100` |
| `GITHUB_TOKEN` | No | GitHub PAT for server-side operations |
| `GITHUB_WEBHOOK_SECRET` | No | Shared secret for verifying incoming GitHub webhooks |
| `ANTHROPIC_API_KEY` | No | LLM access for gateway-initiated operations |
| `ENCRYPTION_KEY` | Yes | Linked from `openforge-web` via `fromService` — must match web and agent |
| `CI_RUNNER_SECRET` | Yes | Linked from `openforge-web` via `fromService` — used for signed CI result callbacks |
| `FORGEJO_INTERNAL_URL` | No | Only needed if using self-hosted Forgejo git |
| `FORGEJO_AGENT_TOKEN` | No | Agent service account token (self-hosted only) |
| `FORGEJO_WEBHOOK_SECRET` | No | Shared secret for verifying incoming Forgejo webhooks (self-hosted only) |

## CI workflow worker (`apps/ci-runner`, service `openforge-ci`)

Deployed as a Render Workflow via the Dashboard (not yet supported in Blueprints).

| Variable | Required | Notes |
|---|---|---|
| `FORGEJO_INTERNAL_URL` | No | Only needed if using self-hosted Forgejo git |
| `FORGEJO_AGENT_TOKEN` | No | Same service token as web/agent, used for authenticated git clones (self-hosted only) |
| `CI_RUNNER_SECRET` | Yes | Linked from `openforge-web`, must match web app for signed callbacks |

## Sandbox service (`packages/sandbox`)

| Variable | Required | Notes |
|---|---|---|
| `SANDBOX_SHARED_SECRET` | Yes | Auto-generated; linked into web and agent via `fromService` |
| `SANDBOX_SESSION_SECRET` | Yes | Auto-generated; session isolation secret |
| `WORKSPACE_ROOT` | No | Default `/workspace` |
| `PORT` | No | Default `3001` |

## Self-hosted Forgejo (optional, local dev only)

Forgejo and MinIO are **not** part of the Render Blueprint. They run locally via `docker-compose.yml` for self-hosted git workflows. If you only use GitHub as your forge provider, you can skip this entirely.

The `docker-compose.yml` defines:
- **`forgejo`** — Forgejo server on port `3000`, backed by Postgres and MinIO
- **`minio`** — S3-compatible storage for Forgejo LFS, attachments, and avatars
- **`postgres`** — shared database (port `5433` to avoid conflicts)

Forgejo-specific environment variables (`FORGEJO__database__*`, `FORGEJO__server__*`, `FORGEJO__storage__*`) are set directly in `docker-compose.yml` and consumed by Forgejo's built-in env config parser. They are not used by the OpenForge application code.

To provision Forgejo for local dev, run `infrastructure/forgejo/setup.ts` after `docker compose up`. It creates admin/agent accounts and emits the `FORGEJO_AGENT_TOKEN` and `FORGEJO_OAUTH_CLIENT_*` values to add to your `.env`.

## Security

See [architecture.md](architecture.md#authentication) for the auth model and invite flow.

- Sandbox: Render Private Service, no public IP, bearer-token auth, path-jailed workspaces, session-scoped secrets.
- All inter-service communication runs over Render's private network.
- LLM API keys saved from **Settings > API Keys** are encrypted in Postgres (AES-256-GCM) using **`ENCRYPTION_KEY`**; the worker decrypts with the same key at runtime. Environment variables remain a supported fallback.
- **`CI_RUNNER_SECRET`** protects **`POST /api/ci/results`**. The Blueprint keeps web and **`openforge-ci`** in sync.

For production hardening: rotate `AUTH_SECRET`, `SANDBOX_SHARED_SECRET`, and **`CI_RUNNER_SECRET`** periodically, and configure branch protection rules on critical repositories.
