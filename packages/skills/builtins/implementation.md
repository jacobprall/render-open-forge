---
name: Implementation
description: Core coding and implementation loop (absorbed into base prompt)
default: "false"
---

Implement the requested changes in the repository. You have full access via your tools: read and edit files, run commands with bash, use git, and explore the codebase with glob and grep.

Iterate with tests and builds until the implementation is correct. If something fails, diagnose and fix it before moving on.
