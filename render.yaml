services:
  # Web Application (Next.js)
  - type: web
    name: openforge-web
    runtime: node
    plan: starter
    autoDeploy: false
    buildCommand: bun install && npx turbo build --filter=@openforge/web
    startCommand: cd apps/web && bun run start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: openforge-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: openforge-redis
          type: keyvalue
          property: connectionString
      - key: FORGEJO_INTERNAL_URL
        value: http://openforge-forgejo:3000
      - key: FORGEJO_EXTERNAL_URL
        sync: false
      - key: FORGEJO_OAUTH_CLIENT_ID
        sync: false
      - key: FORGEJO_OAUTH_CLIENT_SECRET
        sync: false
      - key: FORGEJO_AGENT_TOKEN
        sync: false
      - key: AUTH_SECRET
        generateValue: true
      - key: CSRF_SECRET
        generateValue: true
      - key: AUTH_URL
        sync: false
      - key: NEXT_PUBLIC_APP_URL
        sync: false
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ENCRYPTION_KEY
        generateValue: true
      - key: RENDER_API_KEY
        sync: false
      - key: RENDER_CI_WORKFLOW_SLUG
        value: openforge-ci
      - key: CI_RUNNER_SECRET
        generateValue: true # openforge-ci reads the same value via fromService.envVarKey
      - key: CI_CALLBACK_URL
        sync: false
      - key: NODE_ENV
        value: production

  # Agent Worker
  - type: worker
    name: openforge-agent
    runtime: node
    plan: starter
    autoDeploy: false
    buildCommand: bun install && npx turbo build --filter=@openforge/agent
    startCommand: cd apps/agent && bun run start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: openforge-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: openforge-redis
          type: keyvalue
          property: connectionString
      - key: FORGEJO_INTERNAL_URL
        value: http://openforge-forgejo:3000
      - key: FORGEJO_SANDBOX_URL
        value: http://openforge-forgejo:3000
      - key: FORGEJO_AGENT_TOKEN
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: ENCRYPTION_KEY
        fromService:
          name: openforge-web
          type: web
          envVarKey: ENCRYPTION_KEY
      - key: SANDBOX_SERVICE_HOST
        value: openforge-sandbox:3001
      - key: SANDBOX_SHARED_SECRET
        fromService:
          name: openforge-sandbox
          type: web
          envVarKey: SANDBOX_SHARED_SECRET
      - key: SANDBOX_SESSION_SECRET
        fromService:
          name: openforge-sandbox
          type: web
          envVarKey: SANDBOX_SESSION_SECRET

  # Sandbox (isolated execution environment)
  - type: web
    name: openforge-sandbox
    runtime: docker
    plan: standard
    autoDeploy: false
    dockerfilePath: packages/sandbox/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    disk:
      name: sandbox-workspace
      mountPath: /workspace
      sizeGB: 20
    envVars:
      - key: PORT
        value: "3001"
      - key: WORKSPACE_ROOT
        value: /workspace
      - key: SANDBOX_SHARED_SECRET
        generateValue: true
      - key: SANDBOX_SESSION_SECRET
        generateValue: true

  # MinIO (S3-compatible object storage)
  - type: web
    name: openforge-minio
    runtime: docker
    plan: starter
    autoDeploy: false
    dockerfilePath: infrastructure/minio/Dockerfile
    dockerContext: infrastructure/minio
    healthCheckPath: /minio/health/live
    disk:
      name: minio-data
      mountPath: /data
      sizeGB: 20
    envVars:
      - key: MINIO_ROOT_USER
        generateValue: true
      - key: MINIO_ROOT_PASSWORD
        generateValue: true
      - key: MINIO_DEFAULT_BUCKET
        value: forgejo

  # Forgejo (Internal Forge)
  - type: web
    name: openforge-forgejo
    runtime: docker
    plan: standard
    autoDeploy: false
    dockerfilePath: infrastructure/forgejo/Dockerfile
    dockerContext: infrastructure/forgejo
    healthCheckPath: /api/v1/version
    disk:
      name: forgejo-data
      mountPath: /data
      sizeGB: 10
    envVars:
      - key: FORGEJO__database__DB_TYPE
        value: postgres
      - key: FORGEJO__database__HOST
        fromDatabase:
          name: openforge-db
          property: host
      - key: FORGEJO__database__NAME
        fromDatabase:
          name: openforge-db
          property: database
      - key: FORGEJO__database__USER
        fromDatabase:
          name: openforge-db
          property: user
      - key: FORGEJO__database__PASSWD
        fromDatabase:
          name: openforge-db
          property: password
      - key: FORGEJO__server__ROOT_URL
        sync: false
      - key: FORGEJO__oauth2__JWT_SECRET
        generateValue: true
      - key: GOOGLE_OAUTH_CLIENT_ID
        sync: false
      - key: GOOGLE_OAUTH_CLIENT_SECRET
        sync: false
      # Object storage — route LFS, attachments, avatars, packages to MinIO
      - key: FORGEJO__storage__STORAGE_TYPE
        value: minio
      - key: FORGEJO__storage__MINIO_ENDPOINT
        value: openforge-minio:9000
      - key: FORGEJO__storage__MINIO_ACCESS_KEY_ID
        fromService:
          name: openforge-minio
          type: web
          envVarKey: MINIO_ROOT_USER
      - key: FORGEJO__storage__MINIO_SECRET_ACCESS_KEY
        fromService:
          name: openforge-minio
          type: web
          envVarKey: MINIO_ROOT_PASSWORD
      - key: FORGEJO__storage__MINIO_BUCKET
        value: forgejo
      - key: FORGEJO__storage__MINIO_LOCATION
        value: us-east-1
      - key: FORGEJO__storage__MINIO_USE_SSL
        value: "false"
      - key: PORT
        value: "3000"

  # Gateway (headless REST + MCP API)
  - type: web
    name: openforge-gateway
    runtime: docker
    plan: starter
    autoDeploy: false
    dockerfilePath: apps/gateway/Dockerfile
    dockerContext: .
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: openforge-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: openforge-redis
          type: keyvalue
          property: connectionString
      - key: GATEWAY_API_SECRET
        generateValue: true
      - key: FORGEJO_INTERNAL_URL
        value: http://openforge-forgejo:3000
      - key: FORGEJO_AGENT_TOKEN
        sync: false
      - key: FORGEJO_WEBHOOK_SECRET
        sync: false
      - key: CI_RUNNER_SECRET
        fromService:
          name: openforge-web
          type: web
          envVarKey: CI_RUNNER_SECRET
      - key: ENCRYPTION_KEY
        fromService:
          name: openforge-web
          type: web
          envVarKey: ENCRYPTION_KEY
      - key: GATEWAY_PORT
        value: "4100"
      - key: NODE_ENV
        value: production

  # CI Runner — deploy as a Render Workflow via the Dashboard (not supported in Blueprints yet).
  # Source: apps/ci-runner, build: bun install && npx turbo build --filter=@openforge/ci-runner

  # Redis (job queue + streaming)
  - type: keyvalue
    name: openforge-redis
    plan: starter
    ipAllowList: []
    maxmemoryPolicy: allkeys-lru

databases:
  - name: openforge-db
    plan: basic-256mb
    postgresMajorVersion: "16"